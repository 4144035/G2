# PLAN.md

## ① 目標

建立一個「美股半導體財報事件波動分析工具」，以 Python 自動分析以下 5 檔美國半導體股票：

- NVDA
- AMD
- INTC
- QCOM
- MU

分析期間採「程式執行日往回推兩年」的滾動期間。工具使用 SEC EDGAR 的 10-Q、10-K `filingDate` 作為財報事件日期，結合 Yahoo Finance 日線股價，計算每次事件後第 1、3、5 個交易日的價格變化。

主要目標：

1. 計算各股票在歷史財報事件後 1、3、5 個交易日的絕對波動。
2. 以歷史中位數作為主要波動參考，平均數作為輔助資訊。
3. 保留正負報酬，統計上漲、下跌次數及比例。
4. 比較 5 家公司在財報事件附近的歷史波動差異。
5. 讓工具可重複執行，自動更新至最新兩年資料。
6. 將整理後的資料與圖表交由 AI 產生結論報告。

本工具提供歷史事件波動參考，不預測股價方向，也不提供投資建議。

---

## ② 資料（來源網址、格式、已知的坑）

### A. SEC EDGAR 財報事件資料

來源：SEC EDGAR `data.sec.gov`

公司 submissions API：

```text
https://data.sec.gov/submissions/CIK##########.json
```

主要欄位：

- `name`
- `form`
- `filingDate`
- `accessionNumber`
- `primaryDocument`

只保留 `10-Q`、`10-K`，分析期間為程式執行日往前推兩年至執行日。SEC 原始 JSON 先原封不動保存，再另行清洗。

已知問題與處理方式：

1. 股票代號需先轉換成 CIK，且 URL 中的 CIK 必須補滿 10 碼。
2. 設定包含聯絡資訊的 `User-Agent`，並控制請求頻率。
3. `filingDate` 是 SEC 申報日期，可能不同於公司正式公布財報的時間。
4. 第一版不判斷盤前或盤後申報。
5. 若申報日不是交易日，保留原始日期，計算時對應其後最近的有效交易日。

### B. Yahoo Finance 股價資料

透過 Python `yfinance` 取得 NVDA、AMD、INTC、QCOM、MU 的日線資料，主要欄位為：

- Date
- Open
- High
- Low
- Close
- Volume

分析以 `Close` 為主。股價抓取區間需比事件期間更長，以涵蓋基準日與事件後第 5 個交易日。

已知問題與處理方式：

1. Yahoo Finance 並非官方交易所資料源，介面可能調整。
2. 週末與休市日沒有資料，必須依交易日序列計算第 1、3、5 日。
3. 最新事件若尚無足夠後續交易日，缺值保留為空值，不以 0% 代替。
4. 所有日期欄位統一成可比較的日期格式，並檢查重複值與缺失值。

---

## ③ 方法與計算規則

1. 從 SEC submissions 取出近兩年的 10-Q、10-K 事件。
2. 依各事件的 `filingDate`，在股價交易日序列中找出當日；若當日無交易，改用其後第一個交易日作為事件交易日。
3. 以前一個有效交易日的收盤價作為基準價。
4. 計算事件後第 1、3、5 個交易日報酬：

```text
報酬率(%) =（事件後第 N 個交易日收盤價 ÷ 基準收盤價 - 1）× 100
```

5. 絕對波動為報酬率的絕對值。
6. 各股票分別統計 1、3、5 日報酬率的中位數、平均數、絕對波動中位數、上漲次數、下跌次數與上漲比例。
7. 保留逐事件明細，確保所有彙總數字可追溯。

---

## ④ 程式與檔案結構

預計專案結構：

```text
project-root/
├── PLAN.md
├── TASK.md
├── MEMORY.md
├── requirements.txt
├── README.md
├── src/
│   ├── fetch_sec.py
│   ├── fetch_prices.py
│   ├── analyze_events.py
│   └── generate_report.py
├── data/
│   ├── raw/
│   │   ├── sec/
│   │   └── prices/
│   └── processed/
├── outputs/
│   ├── tables/
│   ├── charts/
│   └── report/
└── tests/
```

開發採分關卡方式：每一關先更新 `TASK.md`，完成後依驗收條件檢查，再更新 `MEMORY.md` 並 commit、push。

---

## ⑤ 輸出成果

1. SEC 原始 submissions JSON。
2. 各股票原始日線價格資料。
3. 清洗後的財報事件表。
4. 逐事件 1、3、5 個交易日報酬與絕對波動表。
5. 各股票的彙總統計表與 5 家公司比較表。
6. 至少包含公司間波動比較及事件後不同期間比較的圖表。
7. AI 結論報告，說明觀察結果、資料限制與方法限制。
8. README 操作說明及可重現的相依套件清單。

輸出表格以 CSV 為主，圖表以 PNG 為主，報告以 Markdown 為主。

---

## ⑥ 驗收、限制與專案維護

### 驗收條件

1. 5 檔股票皆能取得近兩年 SEC 10-Q、10-K 事件與 Yahoo Finance 股價。
2. SEC 原始回應與清洗資料分開保存，且不覆寫原始資料。
3. 事件日遇非交易日時，能正確對應其後最近交易日。
4. 第 1、3、5 日以交易日而非日曆日計算。
5. 缺少後續交易日的事件顯示缺值，不誤填為 0。
6. 任選事件可由輸出結果反算，報酬率與程式結果一致。
7. 圖表、統計表與逐事件明細數值一致。
8. 專案可依 README 在新環境中重新執行。

### 限制

- SEC `filingDate` 不等同精確的 earnings announcement timestamp。
- 第一版不區分盤前、盤中或盤後發布。
- 歷史樣本約兩年，事件數有限，統計結果僅供探索分析。
- Yahoo Finance 資料可能受來源介面或調整方式影響。
- 結果不構成投資建議。

### 專案維護原則

每完成一個關卡：

1. 更新 `MEMORY.md`。
2. 記錄完成內容、遇到的錯誤與解法。
3. 依 `TASK.md` 的驗收條件檢查。
4. commit。
5. push 至 GitHub。

